---
import Header from "../components/Header.astro";
import BlogCard from "../components/BlogCard.astro";
import { getCollection } from "astro:content";

const allPosts = (await getCollection("posts"))
  .filter(p => !p.data.draft)
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<!DOCTYPE html>
<html lang="en">
  <Header title="Blog | Firdaus Nuur Rhamadhan" />
  <body class="bg-[#FFF8F0] text-gray-900">
    <div class="max-w-5xl mx-auto px-6 py-12">
      
      <!-- Header -->
      <div class="mb-12">
        <a href="/" class="inline-flex items-center gap-2 mb-6 px-4 py-2 bg-white border-3 border-black hover:shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] hover:-translate-y-1 transition-all">
          <span class="text-xl">←</span>
          <span class="font-bold">Back to Home</span>
        </a>
        
        <h1 class="text-5xl font-bold mb-4">Blog</h1>
        <p class="text-xl text-gray-600">Thoughts, tutorials, and insights on IoT, Software Development, and Technology</p>
      </div>

      <!-- Blog Posts Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {allPosts.map((post) => (
          <BlogCard 
            title={post.data.title}
            description={post.data.description}
            date={post.data.date}
            author={post.data.author}
            image={post.data.image}
            tags={post.data.tags}
            slug={post.slug}
          />
        ))}
      </div>

      {allPosts.length === 0 && (
        <div class="text-center py-16">
          <div class="inline-block px-8 py-4 bg-white border-3 border-black">
            <p class="text-2xl font-bold mb-2">📝 No blog posts yet</p>
            <p class="text-gray-600">Stay tuned for upcoming articles!</p>
          </div>
        </div>
      )}

    </div>

    <style>
      .border-3 {
        border-width: 3px;
      }
    </style>
  </body>
</html>
